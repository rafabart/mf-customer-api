spring:

  driver-class-name: org.hsqldb.jdbc.JDBCDriver
  url: jdbc:hsqldb:mem:.
  username: sa
  password:

  jpa:
    hibernate.ddl-auto: create-drop

  cloud:
    discovery:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false

  flyway:
    enabled: false