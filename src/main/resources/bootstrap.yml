spring:
  application:
    name: mf-customer-api

  profiles:
    active: ${PROFILE:local}

  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      label: ${BRANCH:local}

management:
  endpoints:
    web:
      exposure:
        include: '*'