spring:
  application:
    name: mf-customer-api

  profiles:
    active: prod

  cloud:
    config:
      uri: ${CONFIG_SERVER_URL}
      label: main

management:
  endpoints:
    web:
      exposure:
        include: '*'