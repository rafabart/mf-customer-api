spring:
  application:
    name: mf-customer-api

  profiles:
    active: ${PROFILE:devlocal}

  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      label: ${BRANCH:devlocal}

management:
  endpoints:
    web:
      exposure:
        include: '*'